html
  head
    title= title
    style(type="text/css").
      body {
        font-size: 15vw;
        display: flex;
        align-items: center;
        justify-content: center;
      }
  body(style='background-color:#' + hexColor)
    div &##{emojiCode}
