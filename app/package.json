{
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.3.3"
  },
  "name": "app",
  "version": "1.0.0",
  "description": "OpenFeature KubeCon Demo",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "start": "npm run build && cp -R public/ dist/ && node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
